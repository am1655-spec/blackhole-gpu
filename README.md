# blackhole-gpu

GPU-accelerated port of the old Java/JavaFX black hole sim (Schwarzschild
geodesic ray tracing), using Taichi so it runs on CUDA (the RTX 2070) or
Vulkan (the laptop's Arc iGPU) without caring which.

No obligations attached to this repo. See `INSTRUCTIONS.md` for setup
and the phased plan.
